## Data Clearing
```df.shape```

Extract columns: ```df = df[['', '', '']]```

Shows the first x columns: ```df.head()```

Group by columns: ```df.groupby(['', ''])```

Sort by: ```df.sort_values('', ascending=False)```

Filters: ```df.isin([COLUMNS])```

